# Modules:
    1. build-in
    2. External

# Variables :
- A variable is a named container used to store data.
- Python is a dynamically typed language, so the variable type is assigned automatically based on the value.

# Typecasting:
The conversion of one digit type into other data type is known as type casting.

Types:
    1. Implicit : In implicit python converts a smaller data type to a higher data type To prevent data loss
    2. Explicit